// import useAxios from "@/hooks/useAxios";
// import { SWRConfiguration } from "swr";

// export const configSWR: SWRConfiguration = {
//   fetcher: (url) => useAxios.get(url),
//   shouldRetryOnError: true,
//   errorRetryCount: 3,
// };
